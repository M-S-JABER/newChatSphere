# Core application
NODE_ENV=development
PORT=5000
HOST=0.0.0.0
LOG_LEVEL=info
# LOG_PRETTY=true
ENFORCE_HTTPS=false

# Database connectivity
DATABASE_URL=postgres://chatsphere:strongpassword@localhost:5432/chatsphere

# Session management
SESSION_SECRET=please-change-me-to-a-long-random-string

# Admin bootstrap (optional overrides for first run)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me-strong

# Meta / WhatsApp provider credentials (optional if managing via UI)
META_TOKEN=
META_PHONE_NUMBER_ID=
META_WABA_ID=
META_VERIFY_TOKEN=
META_APP_SECRET=
# Default template message (optional)
META_TEMPLATE_NAME=
META_TEMPLATE_LANGUAGE=en_US
# META_TEMPLATE_COMPONENTS=[{"type":"body","parameters":[{"type":"text","text":"Your value"}]}]
# Template catalog list (optional)
# META_TEMPLATE_CATALOG=[{"name":"order_update","language":"en_US","description":"Order update template","components":[{"type":"body","parameters":[{"type":"text","text":"Sample"}]}]}]

# Public URLs (optional; required when serving assets via CDN/proxy)
# PUBLIC_BASE_URL=http://localhost:5000
# MEDIA_PUBLIC_BASE_URL=http://localhost:5000
# Webhook URL (optional; required for Meta WhatsApp integration)
# WEBHOOK_URL=http://localhost:5000/api/webhook/meta
# docker run --name whatsapp-db -e POSTGRES_PASSWORD= -e POSTGRES_DB= -p 5432:5432 -d postgres
# npm run db:push
# npm run seed:admin
# --- Server ---
# NODE_ENV=production
# PORT=5000
# # --- Database ---
# DATABASE_URL=postgres://username:strongpassword@localhost:5432/dbname
# # --- Drizzle / App secrets ---
# FILES_SIGNING_SECRET=replace_me_with_strong_secret
# SESSION_SECRET=replace_me_with_strong_secret
# JWT_SECRET=replace_me_with_strong_secret

# # --- Vite (واجهة) ---
# # مثال: VITE_API_BASE_URL لو واجهتك تستدعي API داخلي
# VITE_API_BASE_URL=/api
